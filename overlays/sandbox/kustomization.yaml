---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

patches:
  - target:
      kind: HTTPRoute
      name: home-assistant
    patch: |-
      - op: add
        path: /spec/hostnames/-
        value: "home-assistant.dudo.dev"
